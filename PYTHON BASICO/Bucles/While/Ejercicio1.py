# editando el codigo anterior
"""
for numero in range(1,11):
    print(f"\nestoy imprimiendo {numero}")

    pregunta = input("\n¿Quieres imprimir mas numeros? s/n: ")           
    if pregunta != "s": # si responde algo diferente a S (se considera que respondió n)
        print("Oki, adios\n")
        break # asi termina el ciclo
    else: # sino, vuelve a preguntar 
        print(pregunta)

    # el ciclo se repite hasta llegar a 10
    # pero, y si quiero que sea ilimitado?, en ese caso debería usar un while true :)
"""

print("olaa")
#while True:
